import { LucideIcon } from "lucide-react";

export interface Service {
    id: string;
    title: string;
    content: string;
    icon: string;
    featured_image: string;
    createdAt: string;
    updatedAt: string;
    status: string;
}